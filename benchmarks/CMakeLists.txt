# Benchmark targets will be added here once core index operations exist.
# When added, link them PRIVATE against talus::talus and talus_dev_options so
# they inherit the project warning/sanitizer flags.
